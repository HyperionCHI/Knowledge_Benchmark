import { currentSession } from "../../../lib/auth";
import { audit, ensureWorkspaceRecordSchema } from "../../../../db/workspace-records";
import { readWorkspaceState } from "../../../../db/workspace";
import { sqlite } from "../../../../db/local";

async function admin(request: Request) { const session = await currentSession(request); if (!session?.user) return { denied: Response.json({ error: "请先登录。" }, { status: 401 }), user: null }; if ((session.user as typeof session.user & { role?: string }).role !== "admin") return { denied: Response.json({ error: "仅管理员可维护回收站。" }, { status: 403 }), user: null }; ensureWorkspaceRecordSchema(await readWorkspaceState()); return { denied: null, user: session.user }; }

export async function GET(request: Request) { const access = await admin(request); if (access.denied) return access.denied; const docs = sqlite.prepare(`SELECT d.id, d.title, CASE WHEN s.kind = 'sop' THEN 'sop' ELSE 'doc' END AS type, d.deleted_at AS deletedAt FROM knowledge_documents d JOIN knowledge_spaces s ON s.id = d.space_id WHERE d.deleted_at IS NOT NULL ORDER BY d.deleted_at DESC`).all(); const links = sqlite.prepare("SELECT id, name AS title, 'tracker-link' AS type, deleted_at AS deletedAt FROM tracker_links WHERE deleted_at IS NOT NULL ORDER BY deleted_at DESC").all(); return Response.json({ items: [...docs, ...links].sort((a, b) => String((b as { deletedAt: string }).deletedAt).localeCompare(String((a as { deletedAt: string }).deletedAt))) }); }

export async function POST(request: Request) { const access = await admin(request); if (access.denied || !access.user) return access.denied; const body = await request.json() as { type?: "doc" | "sop" | "tracker-link"; id?: string }; if (!body.id || !["doc", "sop", "tracker-link"].includes(body.type || "")) return Response.json({ error: "恢复参数不正确。" }, { status: 400 }); const table = body.type === "tracker-link" ? "tracker_links" : "knowledge_documents"; const result = sqlite.prepare(`UPDATE ${table} SET deleted_at = NULL, updated_by = ?, updated_at = ?, version = version + 1 WHERE id = ? AND deleted_at IS NOT NULL`).run(access.user.name, new Date().toISOString(), body.id); if (!result.changes) return Response.json({ error: "回收记录不存在。" }, { status: 404 }); audit({ id: access.user.id, name: access.user.name }, "restore", body.type!, body.id); return Response.json({ restored: true }); }
