import { getWorkspaceAccess } from "../../../lib/authorize";
import { canEditWorkspaceScope } from "../../../lib/workspace-permissions";
import { audit, ensureWorkspaceRecordSchema, resolveProductNames, snapshotVersion } from "../../../../db/workspace-records";
import { sqlite } from "../../../../db/local";

type LinkRow = { id: string; brand_id: string; product_id: string; name: string; url: string; cycle: string; platform: string; note: string; color: string; created_at: string; updated_at: string; version: number; sort_order: number };
async function context(request: Request, id: string) { const access = await getWorkspaceAccess(request); if (access.denied || !access.profile || !access.session?.user || !access.state) return { denied: access.denied, access, row: null, target: null }; ensureWorkspaceRecordSchema(access.state); const row = sqlite.prepare("SELECT * FROM tracker_links WHERE id = ? AND deleted_at IS NULL").get(id) as LinkRow | undefined; if (!row) return { denied: Response.json({ error: "链接不存在。" }, { status: 404 }), access, row: null, target: null }; const target = resolveProductNames(access.state, row.product_id); if (!target) return { denied: Response.json({ error: "关联产品不存在。" }, { status: 409 }), access, row: null, target: null }; if (!canEditWorkspaceScope(access.state, access.profile, target.brand, target.product)) return { denied: Response.json({ error: "没有该产品的编辑权限。" }, { status: 403 }), access, row: null, target: null }; return { denied: null, access, row, target }; }

export async function PUT(request: Request, { params }: { params: Promise<{ id: string }> }) {
  const id = (await params).id; const ctx = await context(request, id); if (ctx.denied || !ctx.row || !ctx.target || !ctx.access.session?.user) return ctx.denied;
  const body = await request.json() as { name?: string; url?: string; cycle?: string; platform?: string; note?: string; color?: string; version?: number }; const name = body.name?.trim() || "", url = body.url?.trim() || ""; if (!name || name.length > 15 || typeof body.version !== "number") return Response.json({ error: "链接内容或版本不正确。" }, { status: 400 }); try { new URL(url); } catch { return Response.json({ error: "链接地址无效。" }, { status: 400 }); } if (ctx.row.version !== body.version) return Response.json({ error: "链接已被其他成员修改，请刷新后重试。", conflict: true }, { status: 409 });
  snapshotVersion("tracker-link", id, ctx.row.version, ctx.row, ctx.access.session.user.name); const now = new Date().toISOString(), version = ctx.row.version + 1; sqlite.prepare("UPDATE tracker_links SET name = ?, url = ?, cycle = ?, platform = ?, note = ?, color = ?, updated_by = ?, updated_at = ?, version = ? WHERE id = ? AND version = ?").run(name, url, body.cycle || "daily", body.platform?.trim() || "", body.note?.trim() || "", body.color || "#2859e8", ctx.access.session.user.name, now, version, id, ctx.row.version); audit({ id: ctx.access.session.user.id, name: ctx.access.session.user.name }, "update", "tracker-link", id, name);
  return Response.json({ link: { id, brand: ctx.target.brand, product: ctx.target.product, name, url, cycle: body.cycle || "daily", platform: body.platform?.trim() || "", note: body.note?.trim() || "", color: body.color || "#2859e8", createdAt: ctx.row.created_at, updatedAt: now, updatedBy: ctx.access.session.user.name, version, sortOrder: ctx.row.sort_order } });
}

export async function DELETE(request: Request, { params }: { params: Promise<{ id: string }> }) { const id = (await params).id; const ctx = await context(request, id); if (ctx.denied || !ctx.row || !ctx.access.session?.user) return ctx.denied; snapshotVersion("tracker-link", id, ctx.row.version, ctx.row, ctx.access.session.user.name); const now = new Date().toISOString(); sqlite.prepare("UPDATE tracker_links SET deleted_at = ?, updated_by = ?, updated_at = ?, version = version + 1 WHERE id = ?").run(now, ctx.access.session.user.name, now, id); audit({ id: ctx.access.session.user.id, name: ctx.access.session.user.name }, "delete", "tracker-link", id, ctx.row.name); return Response.json({ deleted: true }); }
