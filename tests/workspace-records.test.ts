import assert from "node:assert/strict";
import { rm } from "node:fs/promises";
import { resolve } from "node:path";

const databasePath = resolve("data/qa-records.sqlite");
const attachmentPath = resolve("data/qa-records-attachments");
process.env.WORKBENCH_DB_PATH = databasePath;
process.env.WORKBENCH_ATTACHMENT_DIR = attachmentPath;
process.env.BETTER_AUTH_URL = "http://localhost:3998";
process.env.BETTER_AUTH_SECRET = "qa-only-secret-for-record-tests-2026";
await Promise.all([databasePath, `${databasePath}-wal`, `${databasePath}-shm`].map((path) => rm(path, { force: true })));
await rm(attachmentPath, { recursive: true, force: true });

const bootstrap = await import("../app/api/auth-bootstrap/route");
const { auth } = await import("../app/lib/auth");
const workspace = await import("../app/api/workspace/route");
const docs = await import("../app/api/docs/route");
const doc = await import("../app/api/docs/[id]/route");
const docCategories = await import("../app/api/docs/categories/route");
const workspaceAttachments = await import("../app/api/workspace-attachments/route");
const workspaceAttachment = await import("../app/api/workspace-attachments/[id]/route");
const workspaceAttachmentVersions = await import("../app/api/workspace-attachments/[id]/versions/route");
const workspaceAttachmentReferences = await import("../app/api/workspace-attachments/[id]/references/route");
const sop = await import("../app/api/sops/[productId]/route");
const sopCategories = await import("../app/api/sops/[productId]/categories/route");
const sopDocuments = await import("../app/api/sop-documents/route");
const sopDocument = await import("../app/api/sop-documents/[id]/route");
const links = await import("../app/api/tracker-links/route");
const search = await import("../app/api/search/route");
const trackerOrder = await import("../app/api/tracker-links/order/route");
const knowledgeOrder = await import("../app/api/knowledge-documents/order/route");
const { scanOrphanAttachments } = await import("../app/lib/orphan-attachments");
const catalog = await import("../app/api/catalog/route");
const catalogOrder = await import("../app/api/catalog/order/route");
const users = await import("../app/api/users/route");
const user = await import("../app/api/users/[id]/route");
const { sqlite } = await import("../db/local");

try {
  await bootstrap.POST(new Request("http://localhost:3998/api/auth-bootstrap", { method: "POST", headers: { "content-type": "application/json" }, body: JSON.stringify({ username: "records_admin", password: "Qa-password-2026", name: "记录管理员" }) }));
  const signedIn = await auth.handler(new Request("http://localhost:3998/api/auth/sign-in/username", { method: "POST", headers: { "content-type": "application/json", origin: "http://localhost:3998" }, body: JSON.stringify({ username: "records_admin", password: "Qa-password-2026" }) }));
  const cookie = signedIn.headers.get("set-cookie")?.split(";", 1)[0]; assert.ok(cookie);
  const headers = { cookie, "content-type": "application/json" };

  let loaded = await workspace.GET(new Request("http://localhost:3998/api/workspace", { headers })); assert.equal(loaded.status, 200);
  const cleanBody = await loaded.json() as { revision: number; state: { brands: string[]; products: string[]; productsByBrand: Record<string, string[]>; brandIds: Record<string, string>; productIds: Record<string, string>; brandIcons: Record<string, string>; productIcons: Record<string, string>; docs: unknown[]; links: unknown[]; sops: unknown[] } & Record<string, unknown> };
  assert.deepEqual(cleanBody.state.brands, [], "新数据库不应预置品牌");
  assert.deepEqual(cleanBody.state.docs, [], "新数据库不应预置知识文档");
  assert.deepEqual(cleanBody.state.links, [], "新数据库不应预置业务链接");
  assert.deepEqual(cleanBody.state.sops, [], "新数据库不应预置 SOP");
  const qaBrands = ["QA 品牌一", "QA 品牌二"], qaProducts = ["QA 产品一", "QA 产品二"];
  cleanBody.state.brands = qaBrands;
  cleanBody.state.products = qaProducts;
  cleanBody.state.productsByBrand = Object.fromEntries(qaBrands.map((brand) => [brand, [...qaProducts]]));
  cleanBody.state.brandIds = Object.fromEntries(qaBrands.map((brand, index) => [brand, `qa-brand-${index + 1}`]));
  cleanBody.state.productIds = Object.fromEntries(qaBrands.flatMap((brand, brandIndex) => qaProducts.map((product, productIndex) => [`${brand}/${product}`, `qa-product-${brandIndex + 1}-${productIndex + 1}`])));
  cleanBody.state.brandIcons = Object.fromEntries(qaBrands.map((brand) => [brand, "building"]));
  cleanBody.state.productIcons = {};
  const configured = await workspace.PUT(new Request("http://localhost:3998/api/workspace", { method: "PUT", headers, body: JSON.stringify({ state: cleanBody.state, revision: cleanBody.revision }) })); assert.equal(configured.status, 200);
  loaded = await workspace.GET(new Request("http://localhost:3998/api/workspace", { headers })); assert.equal(loaded.status, 200);
  const editorCreated = await users.POST(new Request("http://localhost:3998/api/users", { method: "POST", headers, body: JSON.stringify({ username: "records_editor", password: "Qa-editor-password-2026", name: "记录编辑", role: "editor", scopes: ["*"] }) })); assert.equal(editorCreated.status, 201);
  const editorCreatedBody = await editorCreated.json() as { user: { id: string } };
  const editorSignedIn = await auth.handler(new Request("http://localhost:3998/api/auth/sign-in/username", { method: "POST", headers: { "content-type": "application/json", origin: "http://localhost:3998" }, body: JSON.stringify({ username: "records_editor", password: "Qa-editor-password-2026" }) })); assert.equal(editorSignedIn.status, 200);
  const editorCookie = editorSignedIn.headers.get("set-cookie")?.split(";", 1)[0]; assert.ok(editorCookie); const editorHeaders = { cookie: editorCookie, "content-type": "application/json" };
  const viewerCreated = await users.POST(new Request("http://localhost:3998/api/users", { method: "POST", headers, body: JSON.stringify({ username: "records_viewer", password: "Qa-viewer-password-2026", name: "记录查看", role: "viewer", scopes: ["*"] }) })); assert.equal(viewerCreated.status, 201);
  const viewerSignedIn = await auth.handler(new Request("http://localhost:3998/api/auth/sign-in/username", { method: "POST", headers: { "content-type": "application/json", origin: "http://localhost:3998" }, body: JSON.stringify({ username: "records_viewer", password: "Qa-viewer-password-2026" }) })); assert.equal(viewerSignedIn.status, 200);
  const viewerCookie = viewerSignedIn.headers.get("set-cookie")?.split(";", 1)[0]; assert.ok(viewerCookie); const viewerHeaders = { cookie: viewerCookie, "content-type": "application/json" };
  const loadedBody = await loaded.json() as { revision: number; state: { brands: string[]; productsByBrand: Record<string, string[]>; brandIds: Record<string, string>; productIds: Record<string, string>; docCategories: string[]; docCategoryRecords: Array<{ id: string; name: string; parentId: string | null }>; otherDocCategories: string[]; otherDocCategoryRecords: Array<{ id: string; name: string; parentId: string | null }>; docs: Array<{ id: string; version?: number }>; otherDocs: Array<{ id: string; version?: number }>; sops: Array<{ id: string }>; sopCategories: Array<{ id: string; name: string; brand: string; product: string; parentId: string | null }> } };
  const brand = loadedBody.state.brands[0], product = loadedBody.state.productsByBrand[brand][0], productId = loadedBody.state.productIds[`${brand}/${product}`];
  assert.ok(loadedBody.state.brandIds[brand]); assert.ok(productId); assert.equal(loadedBody.state.docs.length, 0);


  const docCategory = loadedBody.state.docCategories[0]; assert.ok(docCategory);
  const rootCategoryIds = loadedBody.state.docCategoryRecords.filter((item) => !item.parentId).map((item) => item.id);
  const reorderedCategories = await knowledgeOrder.PUT(new Request("http://localhost:3998/api/knowledge-documents/order", { method: "PUT", headers, body: JSON.stringify({ kind: "general", entity: "categories", parentId: null, ids: rootCategoryIds.slice().reverse() }) })); assert.ok(reorderedCategories); assert.equal(reorderedCategories.status, 200);
  assert.deepEqual((sqlite.prepare("SELECT id FROM knowledge_categories WHERE space_id = ? AND parent_id IS NULL ORDER BY sort_order, name").all("knowledge-general") as Array<{ id: string }>).map((item) => item.id), rootCategoryIds.slice().reverse());
  const editorReorderedCategories = await knowledgeOrder.PUT(new Request("http://localhost:3998/api/knowledge-documents/order", { method: "PUT", headers: editorHeaders, body: JSON.stringify({ kind: "general", entity: "categories", parentId: null, ids: rootCategoryIds }) })); assert.ok(editorReorderedCategories); assert.equal(editorReorderedCategories.status, 200);
  const createdDoc = await docs.POST(new Request("http://localhost:3998/api/docs", { method: "POST", headers, body: JSON.stringify({ title: "QA 资料", group: docCategory, body: "# QA", items: ["检查"], treeIcon: "book", treeIconColor: "#c54526" }) })); assert.equal(createdDoc.status, 201);
  const createdDocBody = await createdDoc.json() as { doc: { id: string; version: number; treeIcon: string; treeIconColor: string } }; assert.equal(createdDocBody.doc.treeIcon, "book"); assert.equal(createdDocBody.doc.treeIconColor, "#c54526");
  const updatedDoc = await doc.PUT(new Request(`http://localhost:3998/api/docs/${createdDocBody.doc.id}`, { method: "PUT", headers, body: JSON.stringify({ title: "QA 资料 v2", group: docCategory, body: "# QA v2", items: [], treeIcon: "shield", treeIconColor: "#2859E8", version: 1 }) }), { params: Promise.resolve({ id: createdDocBody.doc.id }) }); assert.equal(updatedDoc.status, 200);
  const updatedDocBody = await updatedDoc.json() as { doc: { treeIcon: string; treeIconColor: string } }; assert.equal(updatedDocBody.doc.treeIcon, "shield"); assert.equal(updatedDocBody.doc.treeIconColor, "#2859e8");
  const generalSearchResponse = await search.GET(new Request("http://localhost:3998/api/search?q=QA", { headers })); assert.equal(generalSearchResponse.status, 200);
  const generalSearchBody = await generalSearchResponse.json() as { results: Array<{ title: string; type: string }> };
  assert.ok(generalSearchBody.results.some((item) => item.title === "通用资料与规章制度-QA 资料 v2" && item.type === "文章"));
  const conflict = await doc.PUT(new Request(`http://localhost:3998/api/docs/${createdDocBody.doc.id}`, { method: "PUT", headers, body: JSON.stringify({ title: "冲突", group: docCategory, body: "冲突", items: [], version: 1 }) }), { params: Promise.resolve({ id: createdDocBody.doc.id }) }); assert.equal(conflict.status, 409);
  const attachmentDraft = new FormData(); attachmentDraft.set("scope", "doc"); attachmentDraft.set("documentId", createdDocBody.doc.id); attachmentDraft.set("title", "QA 附件模板"); attachmentDraft.set("summary", "编辑前说明"); attachmentDraft.set("content", "# 编辑前正文"); attachmentDraft.set("file", new File(["qa attachment"], "qa-attachment.txt", { type: "text/plain" }));
  const createdAttachment = await workspaceAttachments.POST(new Request("http://localhost:3998/api/workspace-attachments", { method: "POST", headers: { cookie }, body: attachmentDraft })); assert.ok(createdAttachment); assert.equal(createdAttachment.status, 201);
  const createdAttachmentBody = await createdAttachment.json() as { attachment: { id: string; title: string; content: string; hasAttachment: boolean } }; assert.equal(createdAttachmentBody.attachment.title, "QA 附件模板"); assert.equal(createdAttachmentBody.attachment.hasAttachment, true);
  const attachmentOwner = sqlite.prepare("SELECT document_id FROM workspace_attachments WHERE id = ?").get(createdAttachmentBody.attachment.id) as { document_id: string }; assert.equal(attachmentOwner.document_id, createdDocBody.doc.id);
  const currentArticleLibrary = await workspaceAttachments.GET(new Request(`http://localhost:3998/api/workspace-attachments?scope=doc&documentId=${createdDocBody.doc.id}`, { headers })); assert.equal(currentArticleLibrary.status, 200);
  const currentArticleLibraryBody = await currentArticleLibrary.json() as { attachments: Array<{ id: string }> }; assert.deepEqual(currentArticleLibraryBody.attachments.map((item) => item.id), [createdAttachmentBody.attachment.id]);
  const isolatedDoc = await docs.POST(new Request("http://localhost:3998/api/docs", { method: "POST", headers, body: JSON.stringify({ title: "QA 隔离资料", group: docCategory, body: "# 隔离", items: [] }) })); assert.equal(isolatedDoc.status, 201);
  const isolatedDocBody = await isolatedDoc.json() as { doc: { id: string; version: number } };
  const isolatedArticleLibrary = await workspaceAttachments.GET(new Request(`http://localhost:3998/api/workspace-attachments?scope=doc&documentId=${isolatedDocBody.doc.id}`, { headers })); assert.equal(isolatedArticleLibrary.status, 200);
  const isolatedArticleLibraryBody = await isolatedArticleLibrary.json() as { attachments: Array<{ id: string }> }; assert.deepEqual(isolatedArticleLibraryBody.attachments, []);
  const crossArticleEditDraft = new FormData(); crossArticleEditDraft.set("documentId", isolatedDocBody.doc.id); crossArticleEditDraft.set("title", "不应跨文章更新"); crossArticleEditDraft.set("content", "# 不应保存");
  const rejectedCrossArticleEdit = await workspaceAttachment.PUT(new Request(`http://localhost:3998/api/workspace-attachments/${createdAttachmentBody.attachment.id}`, { method: "PUT", headers: { cookie }, body: crossArticleEditDraft }), { params: Promise.resolve({ id: createdAttachmentBody.attachment.id }) }); assert.ok(rejectedCrossArticleEdit); assert.equal(rejectedCrossArticleEdit.status, 403);
  const rejectedCrossArticleAttachment = await doc.PUT(new Request(`http://localhost:3998/api/docs/${isolatedDocBody.doc.id}`, { method: "PUT", headers, body: JSON.stringify({ title: "QA 隔离资料", group: docCategory, body: "# 隔离", items: [], attachments: [createdAttachmentBody.attachment.id], version: isolatedDocBody.doc.version }) }), { params: Promise.resolve({ id: isolatedDocBody.doc.id }) }); assert.equal(rejectedCrossArticleAttachment.status, 400);
  const attachmentEdit = new FormData(); attachmentEdit.set("scope", "doc"); attachmentEdit.set("documentId", createdDocBody.doc.id); attachmentEdit.set("title", "QA 附件模板 v2"); attachmentEdit.set("summary", "编辑后说明"); attachmentEdit.set("content", "# 编辑后正文"); attachmentEdit.set("file", new File(["qa attachment edit v2"], "qa-attachment-edit-v2.txt", { type: "text/plain" }));
  const updatedAttachment = await workspaceAttachment.PUT(new Request(`http://localhost:3998/api/workspace-attachments/${createdAttachmentBody.attachment.id}`, { method: "PUT", headers: { cookie }, body: attachmentEdit }), { params: Promise.resolve({ id: createdAttachmentBody.attachment.id }) }); assert.ok(updatedAttachment); assert.equal(updatedAttachment.status, 200);
  const updatedAttachmentBody = await updatedAttachment.json() as { attachment: { id: string; title: string; summary: string; content: string; attachmentName: string; hasAttachment: boolean; version: number } }; assert.equal(updatedAttachmentBody.attachment.id, createdAttachmentBody.attachment.id); assert.equal(updatedAttachmentBody.attachment.title, "QA 附件模板 v2"); assert.equal(updatedAttachmentBody.attachment.summary, "编辑后说明"); assert.equal(updatedAttachmentBody.attachment.content, "# 编辑后正文"); assert.equal(updatedAttachmentBody.attachment.attachmentName, "qa-attachment-edit-v2.txt"); assert.equal(updatedAttachmentBody.attachment.hasAttachment, true); assert.equal(updatedAttachmentBody.attachment.version, 2);
  const attachmentSearchResponse = await search.GET(new Request("http://localhost:3998/api/search?q=qa-attachment", { headers })); assert.equal(attachmentSearchResponse.status, 200);
  const attachmentSearchBody = await attachmentSearchResponse.json() as { results: Array<{ title: string; type: string; url?: string }> };
  assert.ok(attachmentSearchBody.results.some((item) => item.title === "通用资料与规章制度-qa-attachment-edit-v2.txt" && item.type === "附件" && item.url === `/api/workspace-attachments/${createdAttachmentBody.attachment.id}`));
  const linkedAttachment = await doc.PUT(new Request(`http://localhost:3998/api/docs/${createdDocBody.doc.id}`, { method: "PUT", headers, body: JSON.stringify({ title: "QA 资料 v2", group: docCategory, body: "# QA v2", items: [], attachments: [createdAttachmentBody.attachment.id], version: 2 }) }), { params: Promise.resolve({ id: createdDocBody.doc.id }) }); assert.equal(linkedAttachment.status, 200);
  const ownerReferences = await workspaceAttachmentReferences.GET(new Request(`http://localhost:3998/api/workspace-attachments/${createdAttachmentBody.attachment.id}/references?documentId=${createdDocBody.doc.id}`, { headers }), { params: Promise.resolve({ id: createdAttachmentBody.attachment.id }) }); assert.equal(ownerReferences.status, 200);
  const ownerReferencesBody = await ownerReferences.json() as { references: Array<{ id: string }> }; assert.ok(ownerReferencesBody.references.some((item) => item.id === createdDocBody.doc.id));
  const rejectedCrossArticleReferences = await workspaceAttachmentReferences.GET(new Request(`http://localhost:3998/api/workspace-attachments/${createdAttachmentBody.attachment.id}/references?documentId=${isolatedDocBody.doc.id}`, { headers }), { params: Promise.resolve({ id: createdAttachmentBody.attachment.id }) }); assert.equal(rejectedCrossArticleReferences.status, 403);
  const readableVersions = await workspaceAttachmentVersions.GET(new Request(`http://localhost:3998/api/workspace-attachments/${createdAttachmentBody.attachment.id}/versions`, { headers }), { params: Promise.resolve({ id: createdAttachmentBody.attachment.id }) }); assert.ok(readableVersions); assert.equal(readableVersions.status, 200);
  const readableVersionsBody = await readableVersions.json() as { versions: Array<{ version: number }> }; assert.deepEqual(readableVersionsBody.versions.map((item) => item.version), [2, 1]);
  const rejectedViewerVersions = await workspaceAttachmentVersions.GET(new Request(`http://localhost:3998/api/workspace-attachments/${createdAttachmentBody.attachment.id}/versions`, { headers: viewerHeaders }), { params: Promise.resolve({ id: createdAttachmentBody.attachment.id }) }); assert.ok(rejectedViewerVersions); assert.equal(rejectedViewerVersions.status, 403);
  const viewerVersionDraft = new FormData(); viewerVersionDraft.set("file", new File(["forbidden version"], "forbidden-version.txt", { type: "text/plain" }));
  const rejectedViewerVersion = await workspaceAttachmentVersions.POST(new Request(`http://localhost:3998/api/workspace-attachments/${createdAttachmentBody.attachment.id}/versions`, { method: "POST", headers: { cookie: viewerCookie }, body: viewerVersionDraft }), { params: Promise.resolve({ id: createdAttachmentBody.attachment.id }) }); assert.ok(rejectedViewerVersion); assert.equal(rejectedViewerVersion.status, 403);
  const adminVersionDraft = new FormData(); adminVersionDraft.set("file", new File(["qa attachment v2"], "qa-attachment-v2.txt", { type: "text/plain" }));
  const createdVersion = await workspaceAttachmentVersions.POST(new Request(`http://localhost:3998/api/workspace-attachments/${createdAttachmentBody.attachment.id}/versions`, { method: "POST", headers: { cookie }, body: adminVersionDraft }), { params: Promise.resolve({ id: createdAttachmentBody.attachment.id }) }); assert.ok(createdVersion); assert.equal(createdVersion.status, 200);
  const createdVersionBody = await createdVersion.json() as { version: number }; assert.equal(createdVersionBody.version, 3);
  const persistedDocStyle = sqlite.prepare("SELECT tree_icon, tree_icon_color FROM knowledge_documents WHERE id = ?").get(createdDocBody.doc.id) as { tree_icon: string; tree_icon_color: string }; assert.deepEqual(persistedDocStyle, { tree_icon: "shield", tree_icon_color: "#2859e8" });
  const deletedAttachmentDoc = await doc.DELETE(new Request(`http://localhost:3998/api/docs/${createdDocBody.doc.id}`, { method: "DELETE", headers }), { params: Promise.resolve({ id: createdDocBody.doc.id }) }); assert.equal(deletedAttachmentDoc.status, 200);
  const deletedOwnerSearchResponse = await search.GET(new Request("http://localhost:3998/api/search?q=qa-attachment", { headers })); assert.equal(deletedOwnerSearchResponse.status, 200);
  const deletedOwnerSearchBody = await deletedOwnerSearchResponse.json() as { results: Array<{ entityId: string }> };
  assert.ok(!deletedOwnerSearchBody.results.some((item) => item.entityId === createdAttachmentBody.attachment.id), "已删除文章的附件不应出现在搜索结果中");
  sqlite.prepare("UPDATE workspace_attachments SET created_at = ? WHERE id = ?").run("2020-01-01T00:00:00.000Z", createdAttachmentBody.attachment.id);
  const orphanRows = await scanOrphanAttachments(); assert.ok(orphanRows.some((item) => item.id === createdAttachmentBody.attachment.id), "仅关联已删除文章的附件应进入孤儿扫描");

  const createdRootCategory = await docCategories.POST(new Request("http://localhost:3998/api/docs/categories", { method: "POST", headers, body: JSON.stringify({ action: "create", name: "QA 一级分类", parentId: null, revision: loadedBody.revision }) })); assert.equal(createdRootCategory.status, 200);
  const rootCategoryBody = await createdRootCategory.json() as { categoryRecords: Array<{ id: string; name: string; parentId: string | null }> };
  const rootCategory = rootCategoryBody.categoryRecords.find((item) => item.name === "QA 一级分类"); assert.ok(rootCategory); assert.equal(rootCategory.parentId, null);
  const createdChildCategory = await docCategories.POST(new Request("http://localhost:3998/api/docs/categories", { method: "POST", headers, body: JSON.stringify({ action: "create", name: "QA 二级分类", parentId: rootCategory.id, revision: loadedBody.revision }) })); assert.equal(createdChildCategory.status, 200);
  const childCategoryBody = await createdChildCategory.json() as { categoryRecords: Array<{ id: string; name: string; parentId: string | null }> };
  const childCategory = childCategoryBody.categoryRecords.find((item) => item.name === "QA 二级分类"); assert.ok(childCategory); assert.equal(childCategory.parentId, rootCategory.id);
  const rejectedThirdLevel = await docCategories.POST(new Request("http://localhost:3998/api/docs/categories", { method: "POST", headers, body: JSON.stringify({ action: "create", name: "QA 三级分类", parentId: childCategory.id, revision: loadedBody.revision }) })); assert.equal(rejectedThirdLevel.status, 400);
  const nestedDoc = await docs.POST(new Request("http://localhost:3998/api/docs", { method: "POST", headers, body: JSON.stringify({ title: "待级联删除资料", group: childCategory.name, body: "# 可恢复正文", items: [] }) })); assert.equal(nestedDoc.status, 201);
  const nestedDocBody = await nestedDoc.json() as { doc: { id: string } };
  const deletedCategory = await docCategories.POST(new Request("http://localhost:3998/api/docs/categories", { method: "POST", headers, body: JSON.stringify({ action: "delete", categoryId: rootCategory.id, name: rootCategory.name, revision: loadedBody.revision }) })); assert.equal(deletedCategory.status, 200);
  const deletedCategoryBody = await deletedCategory.json() as { deletedCategoryIds: string[]; deletedDocumentIds: string[]; categoryRecords: Array<{ id: string }> };
  assert.deepEqual(new Set(deletedCategoryBody.deletedCategoryIds), new Set([rootCategory.id, childCategory.id])); assert.deepEqual(deletedCategoryBody.deletedDocumentIds, [nestedDocBody.doc.id]);
  assert.ok(!deletedCategoryBody.categoryRecords.some((item) => item.id === rootCategory.id || item.id === childCategory.id));
  const recycledNestedDoc = sqlite.prepare("SELECT category_id, deleted_at FROM knowledge_documents WHERE id = ?").get(nestedDocBody.doc.id) as { category_id: string; deleted_at: string | null };
  assert.ok(recycledNestedDoc.deleted_at); assert.notEqual(recycledNestedDoc.category_id, childCategory.id);
  const legacyDocCategory = loadedBody.state.docCategoryRecords.find((item) => item.name === docCategory && !item.parentId); assert.ok(legacyDocCategory);
  const deletedLegacyDocCategory = await docCategories.POST(new Request("http://localhost:3998/api/docs/categories", { method: "POST", headers, body: JSON.stringify({ action: "delete", categoryId: legacyDocCategory.id, name: legacyDocCategory.name, revision: loadedBody.revision }) })); assert.equal(deletedLegacyDocCategory.status, 200);
  const reloadedAfterLegacyDocDelete = await workspace.GET(new Request("http://localhost:3998/api/workspace", { headers })); assert.equal(reloadedAfterLegacyDocDelete.status, 200);
  const reloadedAfterLegacyDocDeleteBody = await reloadedAfterLegacyDocDelete.json() as { state: { docCategoryRecords: Array<{ id: string; name: string }> } };
  assert.ok(!reloadedAfterLegacyDocDeleteBody.state.docCategoryRecords.some((item) => item.id === legacyDocCategory.id || item.name === legacyDocCategory.name), "删除的通用资料分类不应被旧工作区数据重新创建");

  const otherCategory = loadedBody.state.otherDocCategories[0]; assert.equal(otherCategory, "其它资料");
  const createdOtherRoot = await docCategories.POST(new Request("http://localhost:3998/api/docs/categories", { method: "POST", headers, body: JSON.stringify({ space: "other", action: "create", name: "QA 其它一级", parentId: null, revision: loadedBody.revision }) })); assert.equal(createdOtherRoot.status, 200);
  const otherRootBody = await createdOtherRoot.json() as { categoryRecords: Array<{ id: string; name: string; parentId: string | null }> };
  const otherRoot = otherRootBody.categoryRecords.find((item) => item.name === "QA 其它一级"); assert.ok(otherRoot); assert.equal(otherRoot.parentId, null);
  const legacyOtherCategory = loadedBody.state.otherDocCategoryRecords.find((item) => item.name === otherCategory && !item.parentId); assert.ok(legacyOtherCategory);
  const deletedLegacyOtherCategory = await docCategories.POST(new Request("http://localhost:3998/api/docs/categories", { method: "POST", headers, body: JSON.stringify({ space: "other", action: "delete", categoryId: legacyOtherCategory.id, name: legacyOtherCategory.name, revision: loadedBody.revision }) })); assert.equal(deletedLegacyOtherCategory.status, 200);
  const reloadedAfterLegacyOtherDelete = await workspace.GET(new Request("http://localhost:3998/api/workspace", { headers })); assert.equal(reloadedAfterLegacyOtherDelete.status, 200);
  const reloadedAfterLegacyOtherDeleteBody = await reloadedAfterLegacyOtherDelete.json() as { state: { otherDocCategoryRecords: Array<{ id: string; name: string }> } };
  assert.ok(!reloadedAfterLegacyOtherDeleteBody.state.otherDocCategoryRecords.some((item) => item.id === legacyOtherCategory.id || item.name === legacyOtherCategory.name), "删除的其它资料分类不应被旧工作区数据重新创建");
  const createdOtherDoc = await docs.POST(new Request("http://localhost:3998/api/docs?space=other", { method: "POST", headers, body: JSON.stringify({ title: "QA 其它资料", group: otherRoot.name, body: "# 独立其它资料", items: ["独立空间"], treeIcon: "database", treeIconColor: "#0c7c86" }) })); assert.equal(createdOtherDoc.status, 201);
  const createdOtherDocBody = await createdOtherDoc.json() as { doc: { id: string; categoryId: string; version: number; treeIcon: string; treeIconColor: string } }; assert.equal(createdOtherDocBody.doc.categoryId, otherRoot.id); assert.equal(createdOtherDocBody.doc.treeIcon, "database"); assert.equal(createdOtherDocBody.doc.treeIconColor, "#0c7c86");
  const afterOther = await workspace.GET(new Request("http://localhost:3998/api/workspace", { headers })); assert.equal(afterOther.status, 200);
  const afterOtherBody = await afterOther.json() as { state: { docs: Array<{ id: string }>; otherDocs: Array<{ id: string; treeIcon: string; treeIconColor: string }> } };
  assert.ok(afterOtherBody.state.otherDocs.some((item) => item.id === createdOtherDocBody.doc.id && item.treeIcon === "database" && item.treeIconColor === "#0c7c86")); assert.ok(!afterOtherBody.state.docs.some((item) => item.id === createdOtherDocBody.doc.id));

  const savedSop = await sop.PUT(new Request(`http://localhost:3998/api/sops/${productId}`, { method: "PUT", headers, body: JSON.stringify({ content: "# 新 SOP" }) }), { params: Promise.resolve({ productId }) }); assert.ok(savedSop); assert.equal(savedSop.status, 201, "空白产品首次保存时应创建 SOP 总览");
  const sopCategory = loadedBody.state.sopCategories.find((item) => item.brand === brand && item.product === product)?.name || "产品资料";
  const createdSopDocument = await sopDocuments.POST(new Request("http://localhost:3998/api/sop-documents", { method: "POST", headers, body: JSON.stringify({ productId, group: sopCategory, title: "QA 独立 SOP", body: "# 独立文档", items: ["逐篇维护"], treeIcon: "sop", treeIconColor: "#6757f5" }) })); assert.ok(createdSopDocument); assert.equal(createdSopDocument.status, 201);
  const createdSopBody = await createdSopDocument.json() as { sop: { id: string; version: number; treeIcon: string; treeIconColor: string } }; assert.equal(createdSopBody.sop.treeIcon, "sop"); assert.equal(createdSopBody.sop.treeIconColor, "#6757f5");
  const updatedSopDocument = await sopDocument.PUT(new Request(`http://localhost:3998/api/sop-documents/${createdSopBody.sop.id}`, { method: "PUT", headers, body: JSON.stringify({ group: sopCategory, title: "QA 独立 SOP v2", body: "# 独立文档 v2", items: [], treeIcon: "clipboard", treeIconColor: "#923947", version: 1 }) }), { params: Promise.resolve({ id: createdSopBody.sop.id }) }); assert.ok(updatedSopDocument); assert.equal(updatedSopDocument.status, 200);
  const updatedSopBody = await updatedSopDocument.json() as { sop: { treeIcon: string; treeIconColor: string } }; assert.equal(updatedSopBody.sop.treeIcon, "clipboard"); assert.equal(updatedSopBody.sop.treeIconColor, "#923947");
  const sopConflict = await sopDocument.PUT(new Request(`http://localhost:3998/api/sop-documents/${createdSopBody.sop.id}`, { method: "PUT", headers, body: JSON.stringify({ group: sopCategory, title: "冲突", body: "冲突", items: [], version: 1 }) }), { params: Promise.resolve({ id: createdSopBody.sop.id }) }); assert.ok(sopConflict); assert.equal(sopConflict.status, 409);
  const createdSopRoot = await sopCategories.POST(new Request(`http://localhost:3998/api/sops/${productId}/categories`, { method: "POST", headers, body: JSON.stringify({ action: "create", name: "QA SOP 一级", parentId: null }) }), { params: Promise.resolve({ productId }) }); assert.ok(createdSopRoot); assert.equal(createdSopRoot.status, 200);
  const sopRootBody = await createdSopRoot.json() as { categories: Array<{ id: string; name: string; parentId: string | null }> };
  const sopRoot = sopRootBody.categories.find((item) => item.name === "QA SOP 一级"); assert.ok(sopRoot); assert.equal(sopRoot.parentId, null);
  const createdSopChild = await sopCategories.POST(new Request(`http://localhost:3998/api/sops/${productId}/categories`, { method: "POST", headers, body: JSON.stringify({ action: "create", name: "QA SOP 二级", parentId: sopRoot.id }) }), { params: Promise.resolve({ productId }) }); assert.ok(createdSopChild); assert.equal(createdSopChild.status, 200);
  const sopChildBody = await createdSopChild.json() as { categories: Array<{ id: string; name: string; parentId: string | null }> };
  const sopChild = sopChildBody.categories.find((item) => item.name === "QA SOP 二级"); assert.ok(sopChild); assert.equal(sopChild.parentId, sopRoot.id);
  const rejectedSopThirdLevel = await sopCategories.POST(new Request(`http://localhost:3998/api/sops/${productId}/categories`, { method: "POST", headers, body: JSON.stringify({ action: "create", name: "QA SOP 三级", parentId: sopChild.id }) }), { params: Promise.resolve({ productId }) }); assert.ok(rejectedSopThirdLevel); assert.equal(rejectedSopThirdLevel.status, 400);
  const nestedSop = await sopDocuments.POST(new Request("http://localhost:3998/api/sop-documents", { method: "POST", headers, body: JSON.stringify({ productId, group: sopChild.name, title: "待级联删除 SOP", body: "# 可恢复 SOP", items: [] }) })); assert.ok(nestedSop); assert.equal(nestedSop.status, 201);
  const nestedSopBody = await nestedSop.json() as { sop: { id: string } };
  const deletedSopCategory = await sopCategories.POST(new Request(`http://localhost:3998/api/sops/${productId}/categories`, { method: "POST", headers, body: JSON.stringify({ action: "delete", categoryId: sopRoot.id, name: sopRoot.name }) }), { params: Promise.resolve({ productId }) }); assert.ok(deletedSopCategory); assert.equal(deletedSopCategory.status, 200);
  const deletedSopCategoryBody = await deletedSopCategory.json() as { deletedCategoryIds: string[]; deletedDocumentIds: string[]; categories: Array<{ id: string }> };
  assert.deepEqual(new Set(deletedSopCategoryBody.deletedCategoryIds), new Set([sopRoot.id, sopChild.id])); assert.deepEqual(deletedSopCategoryBody.deletedDocumentIds, [nestedSopBody.sop.id]);
  assert.ok(!deletedSopCategoryBody.categories.some((item) => item.id === sopRoot.id || item.id === sopChild.id));
  const recycledNestedSop = sqlite.prepare("SELECT category_id, deleted_at FROM knowledge_documents WHERE id = ?").get(nestedSopBody.sop.id) as { category_id: string; deleted_at: string | null };
  assert.ok(recycledNestedSop.deleted_at); assert.notEqual(recycledNestedSop.category_id, sopChild.id);
  const createdSopFallback = await sopCategories.POST(new Request(`http://localhost:3998/api/sops/${productId}/categories`, { method: "POST", headers, body: JSON.stringify({ action: "create", name: "QA SOP 保留分类", parentId: null }) }), { params: Promise.resolve({ productId }) }); assert.ok(createdSopFallback); assert.equal(createdSopFallback.status, 200);
  const createdSopFallbackBody = await createdSopFallback.json() as { categories: Array<{ id: string; name: string; parentId: string | null }> };
  const sopFallback = createdSopFallbackBody.categories.find((item) => item.name === "QA SOP 保留分类"); assert.ok(sopFallback); assert.equal(sopFallback.parentId, null);
  const legacySopCategory = loadedBody.state.sopCategories.find((item) => item.brand === brand && item.product === product && item.name === sopCategory); assert.ok(legacySopCategory);
  const deletedLegacySopCategory = await sopCategories.POST(new Request(`http://localhost:3998/api/sops/${productId}/categories`, { method: "POST", headers, body: JSON.stringify({ action: "delete", categoryId: legacySopCategory.id, name: legacySopCategory.name }) }), { params: Promise.resolve({ productId }) }); assert.ok(deletedLegacySopCategory); assert.equal(deletedLegacySopCategory.status, 200);
  const reloadedAfterLegacySopDelete = await workspace.GET(new Request("http://localhost:3998/api/workspace", { headers })); assert.equal(reloadedAfterLegacySopDelete.status, 200);
  const reloadedAfterLegacySopDeleteBody = await reloadedAfterLegacySopDelete.json() as { state: { sopCategories: Array<{ id: string; name: string; brand: string; product: string }> } };
  assert.ok(!reloadedAfterLegacySopDeleteBody.state.sopCategories.some((item) => item.brand === brand && item.product === product && (item.id === legacySopCategory.id || item.name === legacySopCategory.name)), "删除的品牌 SOP 分类不应被默认分类初始化重新创建");
  const preservedSopDocument = await sopDocuments.POST(new Request("http://localhost:3998/api/sop-documents", { method: "POST", headers, body: JSON.stringify({ productId, group: sopFallback.name, title: "QA 保留 SOP", body: "# 保留文档", items: [] }) })); assert.ok(preservedSopDocument); assert.equal(preservedSopDocument.status, 201);
  const createdLink = await links.POST(new Request("http://localhost:3998/api/tracker-links", { method: "POST", headers, body: JSON.stringify({ productId, name: "QA 链接", url: "https://example.com", cycle: "other", color: "#2859e8" }) })); assert.ok(createdLink); assert.equal(createdLink.status, 201);
  const createdLinkBody = await createdLink.json() as { link: { id: string } };
  const searchResponse = await search.GET(new Request("http://localhost:3998/api/search?q=QA", { headers })); assert.equal(searchResponse.status, 200);
  const searchBody = await searchResponse.json() as { results: Array<{ title: string; type: string }> };
  assert.ok(searchBody.results.some((item) => item.title === "其它资料-QA 其它资料" && item.type === "文章"));
  assert.ok(searchBody.results.some((item) => item.title === `品牌 / 产品 SOP-${brand}-${product}-QA 保留 SOP` && item.type === "文章"));
  assert.ok(searchBody.results.some((item) => item.title === `项目跟踪表格索引-${brand}-${product}-QA 链接` && item.type === "附件"));
  const ordered = await trackerOrder.PUT(new Request("http://localhost:3998/api/tracker-links/order", { method: "PUT", headers, body: JSON.stringify({ productId, cycle: "other", ids: [createdLinkBody.link.id] }) })); assert.ok(ordered); assert.equal(ordered.status, 200);

  const beforeCatalogOrder = await workspace.GET(new Request("http://localhost:3998/api/workspace", { headers })); assert.equal(beforeCatalogOrder.status, 200);
  const beforeCatalogOrderBody = await beforeCatalogOrder.json() as { revision: number; state: { brands: string[]; productsByBrand: Record<string, string[]>; brandIds: Record<string, string>; productIds: Record<string, string> } };
  const originalBrands = beforeCatalogOrderBody.state.brands, reversedBrands = originalBrands.slice().reverse();
  const adminBrandOrder = await catalogOrder.PUT(new Request("http://localhost:3998/api/catalog/order", { method: "PUT", headers, body: JSON.stringify({ kind: "brands", ids: reversedBrands.map((name) => beforeCatalogOrderBody.state.brandIds[name]), revision: beforeCatalogOrderBody.revision }) })); assert.ok(adminBrandOrder); assert.equal(adminBrandOrder.status, 200);
  const adminBrandOrderBody = await adminBrandOrder.json() as { revision: number };
  const editorBrandOrder = await catalogOrder.PUT(new Request("http://localhost:3998/api/catalog/order", { method: "PUT", headers: editorHeaders, body: JSON.stringify({ kind: "brands", ids: originalBrands.map((name) => beforeCatalogOrderBody.state.brandIds[name]), revision: adminBrandOrderBody.revision }) })); assert.ok(editorBrandOrder); assert.equal(editorBrandOrder.status, 200);
  const editorBrandOrderBody = await editorBrandOrder.json() as { revision: number };
  const orderedBrand = originalBrands[0], originalProducts = beforeCatalogOrderBody.state.productsByBrand[orderedBrand], reversedProducts = originalProducts.slice().reverse();
  const adminProductOrder = await catalogOrder.PUT(new Request("http://localhost:3998/api/catalog/order", { method: "PUT", headers, body: JSON.stringify({ kind: "products", brandId: beforeCatalogOrderBody.state.brandIds[orderedBrand], ids: reversedProducts.map((name) => beforeCatalogOrderBody.state.productIds[`${orderedBrand}/${name}`]), revision: editorBrandOrderBody.revision }) })); assert.ok(adminProductOrder); assert.equal(adminProductOrder.status, 200);
  const adminProductOrderBody = await adminProductOrder.json() as { revision: number };
  const editorProductOrder = await catalogOrder.PUT(new Request("http://localhost:3998/api/catalog/order", { method: "PUT", headers: editorHeaders, body: JSON.stringify({ kind: "products", brandId: beforeCatalogOrderBody.state.brandIds[orderedBrand], ids: originalProducts.map((name) => beforeCatalogOrderBody.state.productIds[`${orderedBrand}/${name}`]), revision: adminProductOrderBody.revision }) })); assert.ok(editorProductOrder); assert.equal(editorProductOrder.status, 200);
  const editorProductOrderBody = await editorProductOrder.json() as { revision: number }; assert.ok(editorProductOrderBody.revision);
  const narrowedEditor = await user.PUT(new Request(`http://localhost:3998/api/users/${editorCreatedBody.user.id}`, { method: "PUT", headers, body: JSON.stringify({ name: "记录编辑", role: "editor", scopes: [`brand:${beforeCatalogOrderBody.state.brandIds[brand]}`] }) }), { params: Promise.resolve({ id: editorCreatedBody.user.id }) }); assert.equal(narrowedEditor.status, 200);
  const scopedSnapshot = await workspace.GET(new Request("http://localhost:3998/api/workspace", { headers })); assert.equal(scopedSnapshot.status, 200); const scopedRevision = Number((await scopedSnapshot.json() as { revision: number }).revision);
  const scopedBrandOrder = await catalogOrder.PUT(new Request("http://localhost:3998/api/catalog/order", { method: "PUT", headers: editorHeaders, body: JSON.stringify({ kind: "brands", ids: [beforeCatalogOrderBody.state.brandIds[brand]], revision: scopedRevision }) })); assert.ok(scopedBrandOrder); assert.equal(scopedBrandOrder.status, 200);
  const scopedBrandOrderBody = await scopedBrandOrder.json() as { revision: number };
  const forbiddenBrand = originalBrands.find((name) => name !== brand); assert.ok(forbiddenBrand);
  const scopedForbiddenProductOrder = await catalogOrder.PUT(new Request("http://localhost:3998/api/catalog/order", { method: "PUT", headers: editorHeaders, body: JSON.stringify({ kind: "products", brandId: beforeCatalogOrderBody.state.brandIds[forbiddenBrand], ids: beforeCatalogOrderBody.state.productsByBrand[forbiddenBrand].map((name) => beforeCatalogOrderBody.state.productIds[`${forbiddenBrand}/${name}`]), revision: scopedBrandOrderBody.revision }) })); assert.ok(scopedForbiddenProductOrder); assert.equal(scopedForbiddenProductOrder.status, 403);
  const viewerCatalogOrder = await catalogOrder.PUT(new Request("http://localhost:3998/api/catalog/order", { method: "PUT", headers: viewerHeaders, body: JSON.stringify({ kind: "brands", ids: originalBrands.map((name) => beforeCatalogOrderBody.state.brandIds[name]), revision: scopedBrandOrderBody.revision }) })); assert.ok(viewerCatalogOrder); assert.equal(viewerCatalogOrder.status, 403);
  const currentRootCategoryIds = (sqlite.prepare("SELECT id FROM knowledge_categories WHERE space_id = ? AND parent_id IS NULL ORDER BY sort_order, name").all("knowledge-general") as Array<{ id: string }>).map((item) => item.id);
  const viewerKnowledgeOrder = await knowledgeOrder.PUT(new Request("http://localhost:3998/api/knowledge-documents/order", { method: "PUT", headers: viewerHeaders, body: JSON.stringify({ kind: "general", entity: "categories", parentId: null, ids: currentRootCategoryIds }) })); assert.ok(viewerKnowledgeOrder); assert.equal(viewerKnowledgeOrder.status, 403);
  const viewerTrackerOrder = await trackerOrder.PUT(new Request("http://localhost:3998/api/tracker-links/order", { method: "PUT", headers: viewerHeaders, body: JSON.stringify({ productId, cycle: "other", ids: [createdLinkBody.link.id] }) })); assert.ok(viewerTrackerOrder); assert.equal(viewerTrackerOrder.status, 403);

  async function uploadCatalogIcon(contents: string, name: string) {
    const draft = new FormData(); draft.set("scope", "catalog-icon"); draft.set("file", new File([contents], name, { type: "image/png" }));
    const response = await workspaceAttachments.POST(new Request("http://localhost:3998/api/workspace-attachments", { method: "POST", headers: { cookie: cookie as string }, body: draft })); assert.ok(response); assert.equal(response.status, 201);
    return (await response.json() as { attachment: { id: string; url: string } }).attachment;
  }
  const brandCatalogIcon = await uploadCatalogIcon("brand catalog icon", "brand-catalog-icon.png");
  const productCatalogIcon = await uploadCatalogIcon("product catalog icon", "product-catalog-icon.png");
  const hiddenCatalogIcon = await uploadCatalogIcon("hidden catalog icon", "hidden-catalog-icon.png");
  const unlinkedCatalogIcon = await workspaceAttachment.GET(new Request(`http://localhost:3998/api/workspace-attachments/${brandCatalogIcon.id}`, { headers: editorHeaders }), { params: Promise.resolve({ id: brandCatalogIcon.id }) }); assert.equal(unlinkedCatalogIcon.status, 403);
  const iconSnapshot = await workspace.GET(new Request("http://localhost:3998/api/workspace", { headers })); assert.equal(iconSnapshot.status, 200);
  const iconSnapshotBody = await iconSnapshot.json() as { revision: number; state: { brandIcons: Record<string, string>; productIcons: Record<string, string> } };
  iconSnapshotBody.state.brandIcons[brand] = `custom:${brandCatalogIcon.url}`;
  iconSnapshotBody.state.productIcons[`${brand}/${product}`] = `custom:${productCatalogIcon.url}`;
  iconSnapshotBody.state.brandIcons[forbiddenBrand] = `custom:${hiddenCatalogIcon.url}`;
  const savedCatalogIcons = await workspace.PUT(new Request("http://localhost:3998/api/workspace", { method: "PUT", headers, body: JSON.stringify({ state: iconSnapshotBody.state, revision: iconSnapshotBody.revision }) })); assert.equal(savedCatalogIcons.status, 200);
  const switchedBrandIcon = await workspaceAttachment.GET(new Request(`http://localhost:3998/api/workspace-attachments/${brandCatalogIcon.id}`, { headers: editorHeaders }), { params: Promise.resolve({ id: brandCatalogIcon.id }) }); assert.equal(switchedBrandIcon.status, 200); assert.equal(await switchedBrandIcon.text(), "brand catalog icon");
  const switchedProductIcon = await workspaceAttachment.GET(new Request(`http://localhost:3998/api/workspace-attachments/${productCatalogIcon.id}`, { headers: editorHeaders }), { params: Promise.resolve({ id: productCatalogIcon.id }) }); assert.equal(switchedProductIcon.status, 200); assert.equal(await switchedProductIcon.text(), "product catalog icon");
  const forbiddenCatalogIcon = await workspaceAttachment.GET(new Request(`http://localhost:3998/api/workspace-attachments/${hiddenCatalogIcon.id}`, { headers: editorHeaders }), { params: Promise.resolve({ id: hiddenCatalogIcon.id }) }); assert.equal(forbiddenCatalogIcon.status, 403);

  const usage = await catalog.GET(new Request(`http://localhost:3998/api/catalog?kind=product&id=${encodeURIComponent(productId)}`, { headers })); assert.equal(usage.status, 200);
  const usageBody = await usage.json() as { usage: { links: number; sops: number } }; assert.ok(usageBody.usage.links >= 1); assert.ok(usageBody.usage.sops >= 1);
  const auditCount = sqlite.prepare("SELECT COUNT(*) AS total FROM audit_logs").get() as { total: number }; const versionCount = sqlite.prepare("SELECT COUNT(*) AS total FROM content_versions").get() as { total: number };
  assert.ok(auditCount.total >= 8); assert.ok(versionCount.total >= 2);
} finally {
  sqlite.close();
  await Promise.all([databasePath, `${databasePath}-wal`, `${databasePath}-shm`].map((path) => rm(path, { force: true })));
  await rm(attachmentPath, { recursive: true, force: true });
}

console.log("workspace-records: ok");
